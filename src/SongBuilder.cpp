/**
 * @file SongBuilder.cpp
 *
 * This is only a dummy file to keep build automisation as simple as possible,
 * since template-classes can not have separate header and implementation
 * files.
 */

#include "SongBuilder.h"
